version: '3'

services:

  chromecast:
    build: 
      context: .
      dockerfile: ./Dockerfile
    restart: unless-stopped
    command: catt --device 192.168.86.45 cast_site 'https://redleg.app'
    #command: catt scan