version: '3.5'

services:

  seq.logging:
    image: datalust/seq:latest
    container_name: seq.logging
    environment:
      - ACCEPT_EULA=Y
    volumes:
      - ./data:/data
    ports:
      - "8080:80" # UI
